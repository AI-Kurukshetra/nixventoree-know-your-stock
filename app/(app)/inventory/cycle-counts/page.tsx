import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function CycleCountsPage() {
  return <PlaceholderPage eyebrow="Stock" title="Cycle counts" description="Add scheduled counts, worksheets, counted quantities, and variance approval flows on this route." />;
}