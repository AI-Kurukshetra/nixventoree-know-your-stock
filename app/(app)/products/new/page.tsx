import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function NewProductPage() {
  return <PlaceholderPage eyebrow="Catalog" title="Create product" description="Add the form and server action for product creation here. The schema already anticipates variants, bundles, barcodes, and preferred suppliers." />;
}