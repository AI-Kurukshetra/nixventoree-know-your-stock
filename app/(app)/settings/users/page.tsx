import { PlaceholderPage } from "@/components/shared/placeholder-page";

export default function UsersSettingsPage() {
  return <PlaceholderPage eyebrow="Settings" title="Users and roles" description="Invite users, assign roles, and map permission groups here once auth flows are connected." />;
}